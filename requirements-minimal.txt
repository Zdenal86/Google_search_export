# Minimal runtime dependencies suitable for CI & lightweight installs. Prefer
# ranges for packages that publish wheels requiring newer Python versions.

# Web framework
streamlit==1.50.0

# Google API
google-api-python-client==2.184.0

# Data processing: allow pandas 1.x or 2.x so CI on older Python versions can
# still install. If your app requires pandas 2.x features, pin explicitly.
pandas>=1.5,<3
beautifulsoup4==4.14.0

# Testing (kept light in minimal; full set in requirements-dev.txt)
pytest>=7.4,<9
pytest-cov>=4.0,<8

# Optional: Selenium for fallback (commented by default)
# selenium==4.35.0
# webdriver-manager==4.0.2
